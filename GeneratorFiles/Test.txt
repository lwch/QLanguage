%token "[" "]" "," ";";

%start start;

array_lst -> [array_lst] "[" "{Digit}" "]"
          ;

start -> declare
      ;

declare -> declare_desc ";"
        ;

declare_desc -> declare_desc "," "{Letter}" array_lst
             |  "{Letter}" "{Letter}" array_lst
             |  "{Letter}" "{Letter}"
             ;